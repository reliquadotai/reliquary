# Keep this image deliberately independent from the validator/GPU dependency
# graph. Every transitive artifact is hash-pinned in the generated lock file.
pydantic==2.13.4
httpx==0.28.1
fastapi==0.141.1
uvicorn==0.52.4
